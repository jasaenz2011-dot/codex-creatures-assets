Placeholder — run `npm run tauri icon <path-to-source-image>` from
`apps/desktop` to generate the real icon set referenced by `tauri.conf.json`
before packaging a release build. Not required for `tauri dev`.
